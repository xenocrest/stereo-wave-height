# 双目水面高度项目：从视频到高度（汇报版）

## 一句话说明

两台相机从不同位置同时看水面。软件先找到“同一个点在两张照片中的位置差”，再用三角几何算出这个点离相机多远，最后把所有点的高度画成一张图。

## 1. 输入和坐标

- 左图 `I_L`、右图 `I_R`：同一时刻的两幅图像。
- 像素 `(u,v)`：图片中的横、纵位置，单位是 pixel。
- 空间点 `(X,Y,Z)`：相机坐标系中的三维位置，单位是 m。
- 基线 `B`：两台相机光心之间的距离，单位是 m。
- 焦距 `f`：标定得到的像素焦距，单位是 pixel。

标定得到 `K0,D0,K1,D1,R,T`。其中 `R,T` 描述两相机的刚体关系；它们来自 OpenCV 双目标定，不能用手工测量值替代。

## 2. 图像校正

镜头会有弯曲，左右相机也可能不完全水平。校正（rectification）把两幅图变成“同一水平线上的照片”。这一步只改变图像坐标表达，不创造三维信息。

## 3. 视差：同一点移动了多少

匹配模型寻找同一物理点在左右图中的位置：

`d = u_L - u_R`

`d` 叫视差。近处物体在两张图中移动得更多，远处物体移动得更少。WASS 或其他已接入的成熟立体模型负责产生视差/深度；本项目不自行编写匹配算法。

## 4. 三角测量：由视差得到深度

理想平行双目相机满足：

`Z = f B / d`

这里 `Z` 是点到相机的深度。视差越大，`Z` 越小；视差越小，`Z` 越大。这是所有深度数值的基本数学来源。

一般姿态下，软件使用标定后的投影矩阵和 `Q` 矩阵重投影，而不是假设相机绝对平行。输出仍是物理三维点 `(X,Y,Z)`。

## 5. 像素与三维点对应

对每一个有效像素保存：

`(u,v) → (X,Y,Z)`

因此可以查询某个图像像素对应的三维位置。没有有效视差的像素会标记为无效，不填入凭空猜测的数值。

## 6. 水面平面和高度

一帧水面近似为一个平面。用三维点拟合：

`A X + B Y + C Z + D = 0`

取单位法向量 `n=(A,B,C)/sqrt(A²+B²+C²)`，平面上一点为 `P0`。任一点 `P` 相对该平面的有符号高度为：

`H = n · (P - P0)`

这比直接把相机的 `Z` 当作水高更合理，因为它计算的是“垂直于水面的距离”。参考平面可以来自选定的静水帧或预先保存的静水参考面。

## 7. 为什么需要参考面

绝对坐标可能包含相机安装高度和倾角。用静水参考面 `H0(X,Y)` 做差：

`H_wave(X,Y,t) = H(X,Y,t) - H0(X,Y)`

静水区域理论上接近 0；波峰为正、波谷为负（符号由法向量方向统一决定）。

## 8. 稠密高度图

WASS/立体模型通常只能在纹理足够、匹配可靠的位置给出观测点。为了展示连续水面，项目中的 MLS（移动最小二乘）用附近已观测点拟合二次曲面：

`H(X,Y)=aX²+bXY+cY²+dX+eY+f`

距离近的点权重大，距离远的点权重小。支持点太少或几何条件病态时返回 `UNSUPPORTED`，不会伪造高度。输出状态区分：`OBSERVED`、`ESTIMATED`、`UNSUPPORTED`。

## 9. 误差如何传播

由 `Z=fB/d`，视差误差 `Δd` 引起的深度误差近似为：

`ΔZ ≈ (Z²/(fB)) Δd`

所以：距离越远、基线越短、焦距越小，深度对匹配误差越敏感。这也是工程设计 baseline、工作距离和镜头焦距的依据。

## 10. 完整数据流

```text
左右视频
  → 同步选帧
  → 读取 K/D/R/T
  → 图像校正
  → 成熟立体模型（WASS 或官方模型）
  → 视差/深度
  → 三维点 (X,Y,Z)
  → 水面平面
  → 像素-三维对应
  → 参考面差分
  → 每像素高度 H(u,v)
  → 高度图和统计结果
```

## 11. 数值可信规则

1. 标定参数必须来自对应相机，不能跨实验混用。
2. 视频必须左右相机角色明确并建立时间对应。
3. 无有效匹配的像素必须保留空值/状态标记。
4. 估算点必须标记为 `ESTIMATED`，不能冒充 WASS 原始观测。
5. 标尺只做独立验证，不进入匹配、三角测量或高度计算。
6. 手机实验用于验证流程和模型，不代表最终海上工程精度。

## 12. 当前项目状态

WASS 流程、OpenCV 标定接口、像素-三维数据结构和高度公式已经建立。当前演示重点是验证完整数据流；官方 Fast-FoundationStereo 已接入，但在本机打包演示中仍需继续解决运行环境兼容问题。任何失败都应显示为失败，不用旧结果冒充新模型结果。

