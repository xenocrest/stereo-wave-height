# 双目标定视频现场拍摄检查单

## 拍摄前

- 固定双目 rig；六段相关采集结束前不得改变两相机相对位置、角度或基线。
- 擦净镜头，使用正规、平整的黑白棋盘；当前目标为 **9 × 6 内角点、方格边长 20 mm**。
- 两台相机若允许，应减少反复自动对焦；曝光应能分辨黑白格且不能过曝反光。
- 确认左右相机能同时完整看到棋盘。

## 拍摄动作

- 有计划地覆盖九个区域：左上、上中、右上、左中、中央、右中、左下、下中、右下。
- 每个主要区域尽量包含：一个较大尺度、一个中等尺度、一个明显但不过度的倾斜姿态。
- 棋盘应完整入镜、短暂停稳、避免运动模糊，并改变距离和俯仰/偏航角。
- 旧 HomeTank_004 的主要缺陷是顶部、角落和左右边缘覆盖不足，本次需重点补齐。
- 目标不是录得越久越好；短而有计划的覆盖优于长时间在中央重复晃动。

## 拍摄后（现场完成）

先运行快速验收：

```powershell
$env:PYTHONPATH='src;.'
python -m calibration.capture_qa --left <LEFT_CALIBRATION_VIDEO> --right <RIGHT_CALIBRATION_VIDEO> --quick --output <QA_OUTPUT_DIRECTORY>
```

只有结果为 `CAPTURE_READY_FOR_CALIBRATION` 才进入正式标定。若报告提示缺失区域、尺度、姿态、清晰度或双侧检测率不足，应现场补拍；不要直接进入 WASS。
