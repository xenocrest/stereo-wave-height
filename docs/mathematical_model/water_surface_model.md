# 水面模型

瞬时水面可表示为参考平面上的单值高度场

$$z=\eta(x,y,t),$$

适用于无遮挡且相对于参考方向不存在翻卷多值面的区域。离散重建点经过坐标变换、异常点剔除和空间插值后，可形成规则网格 $\eta_{ij}(t)$。

参考水面可由静水标定、稳健平面拟合或长时间均值获得。插值不得跨越无数据区域，输出需保留原始覆盖率和质量掩膜。对破碎浪等非单值区域，应优先保留三维点云表达并标记模型失效条件。
