# 立体重建模型

校正后的理想平行双目系统中，深度近似为

$$Z=\frac{fB}{d},$$

其中 $f$ 为像素单位焦距，$B$ 为基线长度，$d=u_L-u_R$ 为视差。横纵坐标可由

$$X=\frac{(u-c_x)Z}{f_x},\qquad Y=\frac{(v-c_y)Z}{f_y}$$

得到。一般相机配置下使用左右投影矩阵进行三角测量。

重建流程应保留视差、重投影误差、左右一致性和匹配置信度，以支持后续无效点剔除与高度不确定度传播。
