# 双目水面高度项目：从视频到高度（汇报版）

## 一句话说明

两台相机从不同位置同时看水面。软件先找到“同一个点在两张照片中的位置差”，再用三角几何算出这个点离相机多远，最后把所有点的高度画成一张图。

## 1. 输入和坐标

- 左图 `I_L`、右图 `I_R`：同一时刻的两幅图像。
- 像素 `(u,v)`：图片中的横、纵位置，单位是 pixel。
- 空间点 `(X,Y,Z)`：相机坐标系中的三维位置，单位是 m。
- 基线 `B`：两台相机光心之间的距离，单位是 m。
- 焦距 `f`：标定得到的像素焦距，单位是 pixel。

标定得到 `K0,D0,K1,D1,R,T`。其中 `R,T` 描述两相机的刚体关系；它们来自 OpenCV 双目标定，不能用手工测量值替代。

## 2. 图像校正

镜头会有弯曲，左右相机也可能不完全水平。校正（rectification）把两幅图变成“同一水平线上的照片”。这一步只改变图像坐标表达，不创造三维信息。

## 3. 视差：同一点移动了多少

匹配模型寻找同一物理点在左右图中的位置：

`d = u_L - u_R`

`d` 叫视差。近处物体在两张图中移动得更多，远处物体移动得更少。WASS 或其他已接入的成熟立体模型负责产生视差/深度；本项目不自行编写匹配算法。

## 4. 三角测量：由视差得到深度

理想平行双目相机满足：

`Z = f B / d`

这里 `Z` 是点到相机的深度。视差越大，`Z` 越小；视差越小，`Z` 越大。这是所有深度数值的基本数学来源。

一般姿态下，软件使用标定后的投影矩阵和 `Q` 矩阵重投影，而不是假设相机绝对平行。输出仍是物理三维点 `(X,Y,Z)`。

## 5. 像素与三维点对应

对每一个有效像素保存：

`(u,v) → (X,Y,Z)`

因此可以查询某个图像像素对应的三维位置。没有有效视差的像素会标记为无效，不填入凭空猜测的数值。

## 6. 水面平面和高度

一帧水面近似为一个平面。用三维点拟合：

`A X + B Y + C Z + D = 0`

取单位法向量 `n=(A,B,C)/sqrt(A²+B²+C²)`，平面上一点为 `P0`。任一点 `P` 相对该平面的有符号高度为：

`H = n · (P - P0)`

这比直接把相机的 `Z` 当作水高更合理，因为它计算的是“垂直于水面的距离”。参考平面可以来自选定的静水帧或预先保存的静水参考面。

## 7. 为什么需要参考面

绝对坐标可能包含相机安装高度和倾角。用静水参考面 `H0(X,Y)` 做差：

`H_wave(X,Y,t) = H(X,Y,t) - H0(X,Y)`

静水区域理论上接近 0；波峰为正、波谷为负（符号由法向量方向统一决定）。

## 8. 稠密高度图

WASS/立体模型通常只能在纹理足够、匹配可靠的位置给出观测点。为了展示连续水面，项目中的 MLS（移动最小二乘）用附近已观测点拟合二次曲面：

`H(X,Y)=aX²+bXY+cY²+dX+eY+f`

距离近的点权重大，距离远的点权重小。支持点太少或几何条件病态时返回 `UNSUPPORTED`，不会伪造高度。输出状态区分：`OBSERVED`、`ESTIMATED`、`UNSUPPORTED`。

## 9. 误差如何传播

由 `Z=fB/d`，视差误差 `Δd` 引起的深度误差近似为：

`ΔZ ≈ (Z²/(fB)) Δd`

所以：距离越远、基线越短、焦距越小，深度对匹配误差越敏感。这也是工程设计 baseline、工作距离和镜头焦距的依据。

## 10. 完整数据流

```text
左右视频
  → 同步选帧
  → 读取 K/D/R/T
  → 图像校正
  → 成熟立体模型（WASS 或官方模型）
  → 视差/深度
  → 三维点 (X,Y,Z)
  → 水面平面
  → 像素-三维对应
  → 参考面差分
  → 每像素高度 H(u,v)
  → 高度图和统计结果
```

## 11. 数值可信规则

1. 标定参数必须来自对应相机，不能跨实验混用。
2. 视频必须左右相机角色明确并建立时间对应。
3. 无有效匹配的像素必须保留空值/状态标记。
4. 估算点必须标记为 `ESTIMATED`，不能冒充 WASS 原始观测。
5. 标尺只做独立验证，不进入匹配、三角测量或高度计算。
6. 手机实验用于验证流程和模型，不代表最终海上工程精度。

## 12. 当前项目状态

WASS 流程、OpenCV 标定接口、像素-三维数据结构和高度公式已经建立。当前演示重点是验证完整数据流；官方 Fast-FoundationStereo 已接入，但在本机打包演示中仍需继续解决运行环境兼容问题。任何失败都应显示为失败，不用旧结果冒充新模型结果。

## 附录：像教科书一样理解每一步

### A. 为什么两台相机能测距离

把两台相机想成两只眼睛。左眼看到一个点在位置 `u_L`，右眼看到同一个点在位置 `u_R`。两眼看到的位置不一样，差值就是视差 `d`：

`d = u_L - u_R`

设两台相机相距 `B`，镜头焦距为 `f`。相似三角形给出：

`d / f = B / Z`

整理得到：

`Z = fB/d`

这不是经验公式，而是针孔相机几何的直接结果。比如 `f=3000 px`、`B=0.16 m`、`d=240 px`，则 `Z=3000×0.16/240=2.00 m`。

### B. 一张照片里的像素为什么不能单独决定高度

一个像素只告诉我们“一条光线”，并没有告诉我们光线上哪一点是水面。第二台相机提供另一条光线；两条光线的交会位置才给出三维点。标定参数 `K` 把像素变成光线方向，`R,T` 把左右相机放进同一个坐标系。

针孔投影可写成：

`u = f_x X/Z + c_x`，`v = f_y Y/Z + c_y`

反过来，对像素 `(u,v)` 和深度 `Z`：

`X=(u-c_x)Z/f_x`，`Y=(v-c_y)Z/f_y`

因此数据变化顺序是：像素位置 + 视差 → 深度 `Z` → 空间坐标 `X,Y`。

### C. 标定到底测量了什么

内参矩阵：

`K=[[f_x,0,c_x],[0,f_y,c_y],[0,0,1]]`

其中 `f_x,f_y` 是像素焦距，`c_x,c_y` 是主点。畸变向量 `D` 描述镜头边缘的弯曲。外参满足：

`P_right = R P_left + T`

`R` 是旋转，`T` 是平移。OpenCV 双目标定同时利用棋盘真实尺寸和左右角点，求出这些量；WASS 只读取这些结果，不重新猜测相机几何。

### D. 为什么要做校正（rectification）

未经校正时，同一个点可能在左右图的不同斜线上，匹配要在二维区域搜索。校正后，同一点落在同一行，只需沿水平方向搜索 `u`，视差就变成一个标量。这减少了错误匹配，也让 `d` 与公式一一对应。

### E. 从很多三维点找到水面

水面不是一个单独点，而是一大片点。小范围内可用平面表示：

`A X + B Y + C Z + D = 0`

程序用观测到的 `(X,Y,Z)` 求出 `A,B,C,D`，并计算每个点到平面的有符号距离。令 `n=(A,B,C)`，则：

`distance = (A X+B Y+C Z+D)/sqrt(A²+B²+C²)`

距离为正表示在法向量一侧，负表示另一侧。选定静水面为零面后，这个距离就是高度变化 `H`。

### F. 为什么静水参考要单独保存

相机整体移动一点，会让所有 `Z` 一起变化；这不一定是真波浪。用静水帧求参考：

`Z_0(X,Y)=mean_t Z_static(X,Y,t)`

波浪高度为：

`H(X,Y,t)=Z_wave(X,Y,t)-Z_0(X,Y)`

相同坐标变换下，任意共同平移 `C` 都会抵消：

`(Z_wave-C)-(Z_0-C)=Z_wave-Z_0`

这就是参考面差分的数学依据。

### G. 为什么有些像素必须是空的

水面没有纹理、被反光遮住或左右画面不重叠时，匹配没有足够证据。此时填一个数字等于猜测。项目使用三种状态：

- `OBSERVED`：来自真实有效立体匹配；
- `ESTIMATED`：由附近观测点的明确曲面模型估算；
- `UNSUPPORTED`：证据不足，保持 NaN。

MLS 二次曲面模型为：

`H=aX²+bXY+cY²+dX+eY+f`

它通过加权最小二乘求系数，权重可取 `w_i=exp(-r_i²/(2σ²))`。距离 `r_i` 越小，权重越大。邻域点太少、矩阵条件数过大时拒绝输出。

### H. 误差为什么会在远处变大

对 `Z=fB/d` 求导：

`|ΔZ| ≈ (Z²/(fB)) |Δd|`

例如 `Z=2 m,f=3000 px,B=0.16 m,Δd=0.1 px`：

`ΔZ≈4/(480)×0.1=0.000833 m=0.833 mm`

距离变成 4 m 时，误差按 `Z²` 增长到约 3.33 mm。因此工程设计要同时考虑工作距离、基线和焦距，而不是只追求更高分辨率。

### I. 一个完整像素的示例

假设校正后某像素在左图 `u_L=1200`，右图 `u_R=960`，所以 `d=240 px`。若 `f=3000 px,B=0.16 m`，则 `Z=2.00 m`。若主点 `c_x=960,c_y=540`、`f_x=f_y=3000 px`，像素 `(1200,600)` 对应：

`X=(1200-960)×2/3000=0.160 m`

`Y=(600-540)×2/3000=0.040 m`

得到空间点 `(0.160,0.040,2.000) m`。把它代入水面平面距离公式，就得到该像素的水面高度。整幅图对每个有效像素重复同样过程，形成 `H(u,v)` 高度图。

## 项目成果总表（汇报时可直接使用）

### 已完成的基础成果

1. **双目数学模型**：用相似三角形建立了视差、基线、焦距和距离的关系，并完成误差传播分析。
2. **合成数据仿真**：自己生成虚拟相机、静水、固定高度和正弦波，验证了投影和深度公式。
3. **真实相机标定**：用 OpenCV 棋盘格流程获得 `K0/D0/K1/D1/R/T`，并检查了基线和坐标方向。
4. **WASS 接口**：把 OpenCV 标定结果转换为 WASS 所需格式，跑通了 prepare、match、stereo 和点云输出研究流程。
5. **三维点云**：完成 `mesh_cam.xyzC` 读取、坐标和单位记录、平面拟合及规则网格接口研究。
6. **像素—三维对应**：建立每个像素 `(u,v)` 与空间点 `(X,Y,Z)` 的对应关系。
7. **水面高度**：用平面法向距离计算高度，不把相机的 Z 坐标直接冒充水面高度。
8. **空间补全**：用物理坐标中的局部二次曲面 MLS 估算小范围缺口，并在支持不足时明确输出空值。
9. **工程工具**：建立 baseline、工作距离、焦距、视差范围和深度分辨率分析工具。
10. **独立验证**：建立标尺验证接口；标尺只用于检查结果，不进入重建计算。

### 已得到的重要实验认识

- 理想仿真中，`1 px` 视差误差对深度的影响可以由误差传播公式直接计算。
- Case 1 仿真中，WASS 原始支持区域的高度差约为 `8.999 mm`，接近设定的 `10 mm`；大误差主要出现在没有原始观测点、由网格方法补全的区域。
- 手机真实视频可以得到单帧水面点云，但跨帧稳定性受匹配、纹理和视差范围影响，不能把单帧成功解释为工程精度。
- 真实视频阶段已建立完整的输入、标定、同步、WASS、点云、高度和独立验证接口；当前仍需继续提升实际输入下的稳定性。

### 所有核心公式集中版

**投影：**

`u=f_x X/Z+c_x`，`v=f_y Y/Z+c_y`

**反投影：**

`X=(u-c_x)Z/f_x`，`Y=(v-c_y)Z/f_y`

**视差：**

`d=u_L-u_R`

**双目深度：**

`Z=fB/d`

**深度误差传播：**

`ΔZ≈Z²/(fB)·Δd`

**三维刚体变换：**

`P_R=R P_L+T`

**水面平面：**

`AX+BY+CZ+D=0`

**点到平面高度：**

`H=(AX+BY+CZ+D)/sqrt(A²+B²+C²)`

**静水参考差分：**

`H_wave(X,Y,t)=Z_wave(X,Y,t)-Z_static(X,Y)`

**MLS 局部曲面：**

`H(X,Y)=aX²+bXY+cY²+dX+eY+f`

**高斯距离权重：**

`w_i=exp(-r_i²/(2σ²))`

## 汇报时的边界说明

本项目已经证明“从双目图像到水面高度”的数学路线和软件接口是完整、可追溯的；但低成本手机真实视频的跨帧匹配稳定性仍未达到工程验收要求。当前结果用于展示方法、流程和误差来源，不宣称真实海面或工业设备的最终测量精度。
