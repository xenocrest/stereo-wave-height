# HomeTank_006 解算启动记录

Starting HEAD: `93f5fe5c200e5077caf8e801aa682e3aace075c3`

## 已完成的只读检查

- 原始四视频存在，初检均为3840×2160，约30 FPS；角色维持cam0 LEFT、cam1 RIGHT。
- 以显式关闭OpenCV自动旋转、cam0旋转180°/cam1不旋转生成缩略诊断图。原始视频不变。
- 标定抽样时刻：每侧5、20、40、65、90秒。棋盘可见，部分画面棋盘边缘被裁切。尚未进行检测率或标定质量验收。
- 波浪抽样时刻：每侧0、2、4、6、10秒。开头是静水参考候选，不据缩略图断言完全静止。
- 同名时间6秒，左侧已出现拨水工具、右侧尚未出现；这是时间偏移或视角遮挡差异的线索，不足以单独证明或确定精确时间偏移。
- 仓库外诊断图：`D:/stereo-wave-height-runs/HomeTank_006/intake/calibration_contact.jpg` 与 `wave_contact.jpg`。

## 当前前置条件

用户已确认9×6内角点、方格边长0.020 m，已登记manifest。旧 `calibration.run_adaptive` 按相近原始时间配对，不得未经同步检查直接套用新实验。

本次尚未运行标定、WASS、替代重建或高度补全。没有新高度结果，不能宣称工程目标通过。

下一步：确认棋盘物理规格；确定校准视频与波浪视频各自同步关系；筛选停稳且覆盖充分的棋盘观测；新标定及独立检验；冻结静水参考；选定波浪时刻重建并检查观测支持、逐像素估计来源和误差。

## 实际执行结果

使用 `tools/hometank006_initial_analysis.py --mode corners` 调用现有官方检测策略：原生SB、半尺寸SB、原图亚像素细化，约1 Hz抽样。

| 侧别 | 抽样帧数 | 完整54角点帧数 |
| --- | ---: | ---: |
| LEFT | 109 | 1 |
| RIGHT | 109 | 0 |

有限补救 `tools/hometank006_corner_recovery.py`：每侧15个代表时刻，依次经典检测、CLAHE经典检测、四分之一尺寸SB，成功点映射回原图并亚像素细化。没有改变棋盘尺寸或生成不存在的角点。

| 侧别 | 代表帧数 | 恢复帧数 |
| --- | ---: | ---: |
| LEFT | 15 | 8 |
| RIGHT | 15 | 0 |

四个视频逐帧PTS亮度已提取。默认亮度事件检测：calibration左6/右3，wave左2/右0；未建立可靠双侧光源事件同步。亮度导数互相关仅为诊断：calibration候选偏移约-0.30 s（相关系数0.495），wave约-0.05 s（0.293）；wave分段最优偏移不稳定。这些数值不能作为已确认同步输入。

放大右侧65秒图可见手指遮挡棋盘边缘，手绘格子内部灰度不均匀、边界不规则；属于检测失败线索，尚未隔离出唯一因果。4K本身没有解决这些问题。不能从图像确认所有实际方格边长都等于名义20 mm。

当前结论：`BLOCKED_AT_CALIBRATION_OBSERVATIONS`。尚未产生本实验K/D/R/T、XYZ或高度。WASS执行次数0；没有用旧参数、标尺或假角点绕过失败。逐像素工程高度目标尚未完成。

可替代路径：使用图像中真实可见的部分棋盘角点进行带明确物点编号的OpenCV标定；要求先解决角点物点对应、双目同步和独立重投影验证。不得以拟合完整棋盘的虚构角点作为观测。若可见角点和空间姿态不足，需补充标定观测，而不是改用不带尺度的三维结果冒充米制高度。
